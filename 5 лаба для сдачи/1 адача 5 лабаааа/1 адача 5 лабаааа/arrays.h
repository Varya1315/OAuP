#include<stdio.h> 
#include <stdlib.h> 
#include<time.h> 

int startOfSegment();
int endOfSegment();
int sizearr();
int taska(int*, int, int, int);

void printMassiv(int*, int);
void random(int*, int);